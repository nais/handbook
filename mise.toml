[tools]
python = "latest"
uv = "latest"
"go:github.com/sethvargo/ratchet" = "latest"

[tasks.install]
description = "Install dependencies"
run = "uv sync"

[tasks.build]
description = "Build the site"
depends = ["install"]
run = "uv run zensical build --strict --clean"

[tasks.check]
description = "Check the site builds"
depends = ["build"]

[tasks.serve]
description = "Start the development server"
depends = ["install"]
run = "uv run zensical serve"

[tasks.ratchet]
description = "Pin GitHub Actions versions"
run = "ratchet pin .github/workflows/*.yaml"
