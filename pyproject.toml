[project]
name = "handbook"
version = "0.1.0"
description = "Nais team handbook"
requires-python = ">=3.11"
dependencies = [
    "zensical==0.0.44",
]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
