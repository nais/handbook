[tool.poetry]
name = "doc"
version = "0.1.0"
description = "nais handbook"
authors = []

[tool.poetry.dependencies]
python = "^3.7"
mkdocs = "^1.3.1"
Pygments = "^2.7.4"
pymdown-extensions = "^8.0.1"
mkdocs-material = "^7.3.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
